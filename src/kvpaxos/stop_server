#!/usr/bin/python

import os
import sys
import json
import urllib2

if __name__ == "__main__":
	settings = json.loads(open("../../conf/settings.conf").read())
	
	address = "http://" + settings["n"+nodeID] + ":" + settings["port"]

	if len(sys.argv) == 2:
		urllib2.urlopen(address + "/kvman/shutdown")
		quit()
	
	print "Please specify the argument: <node ID>"


